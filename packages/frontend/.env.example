# Copy to .env and fill in values
# Vite reads this file automatically in dev mode

# SSO integration (must match SSO_CLIENT_ID on the backend)
VITE_SSO_ISSUER=https://accounts.freiding.by
VITE_SSO_CLIENT_ID=
